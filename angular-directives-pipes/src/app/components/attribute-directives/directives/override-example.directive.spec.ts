import { OverrideExampleDirective } from './override-example.directive';

describe('OverrideExampleDirective', () => {
  it('should create an instance', () => {
    const directive = new OverrideExampleDirective();
    expect(directive).toBeTruthy();
  });
});
